import React from "react";

const PlaylistCanvas = (props) => {
    const {   
        primaryColor,
        secondaryColor
    } = props

    // const colorChange = () => {
    //
    // }

    return (
        <canvas>Actual Painting</canvas>
    )
}

export default PlaylistCanvas;
